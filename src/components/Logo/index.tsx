import logo from '@/assets/Logo.svg';

export const Logo = () => {
  return (
    <img
      className="img-logo"
      style={{ maxWidth: '50px' }}
      src={logo}
      alt="Matheus Felipe - Logo"
    />
  );
};

